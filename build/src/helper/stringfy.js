"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function recursoToString(object) {
    const result = new Object();
    const properties = Object.keys(result);
    // Object.entr
    // for (const value in result) {
    //     result[value] = object[value];
    // }
}
