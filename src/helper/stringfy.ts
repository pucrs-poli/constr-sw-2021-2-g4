import { IRecurso } from '../models/RecursoModel'
function recursoToString(object: any) {
    const result = new Object() as IRecurso;
    const properties = Object.keys(result);
    // Object.entr
    // for (const value in result) {
    //     result[value] = object[value];
    // }
}